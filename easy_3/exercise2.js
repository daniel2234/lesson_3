// Question 2
// What will the following code output?

console.log([1, 2, 3] + [4, 5]);

//
//the + operator converts the arrays to strings, and then concatenates to strings, so the output is the string 1,2,34, 5